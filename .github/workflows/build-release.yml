name: Build Plugin ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare plugin
        run: |
          mkdir -p build/price-compare-assistant
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' ./ build/price-compare-assistant/

      - name: Create ZIP
        run: |
          cd build
          zip -r price-compare-assistant.zip price-compare-assistant

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: price-compare-assistant
          path: build/price-compare-assistant.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/price-compare-assistant.zip
          generate_release_notes: true
